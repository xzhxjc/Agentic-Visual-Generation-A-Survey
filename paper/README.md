# Paper Sources

This directory contains two manuscript source packages:

- `acm-version/`: the original ACM project;
- `reading-version/`: the single-column reading version.

The source packages preserve their own entry files, section files, bibliography, class files, and build notes. Image binaries are omitted from this public package and are documented in `reading-version/assets/README.md`.
