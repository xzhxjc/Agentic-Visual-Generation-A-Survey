# Scripts

The scripts in this directory support chapter-material generation and translation-source synchronization. They are included as source utilities; generated outputs and local caches are excluded by `.gitignore`.

The scripts expect the local literature metadata directory used during manuscript preparation. That archive is intentionally omitted from this public package, so the scripts require a separately supplied data root before they can run.
