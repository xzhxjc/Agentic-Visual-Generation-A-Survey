# Homepage Assets Placeholder

This directory is intentionally kept free of image binaries for the first public upload.

Planned assets:

- `overview-figure.svg` or `overview-figure.pdf`: the final AVG operating-trajectory figure;
- `ai4gc-logo.*`: AI4GC Lab logo with confirmed redistribution permission;
- `zju-logo.*`: Zhejiang University mark with confirmed redistribution permission;
- `domain-overview.*`: optional domain coverage figure.

The current homepage uses a GitHub-rendered Mermaid diagram, so the repository remains readable before these assets are added.
